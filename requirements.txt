PyMuPDF>=1.23.0
pytesseract>=0.3.0
pdf2image>=1.16.0
Pillow>=9.0.0
numpy>=1.20.0

# Note: pdf2image requires poppler-utils installed on the system (brew install poppler on macOS)
# OCR via Tesseract: brew install tesseract
